<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Chakra+Petch:ital,wght@0,300;0,400;0,500;0,600;0,700;1,300;1,400;1,500;1,600;1,700&family=Space+Grotesk:wght@300..700&display=swap"
        rel="stylesheet">
    <link rel="stylesheet" href="../styles/style.css">
    <title>Acceso Administrador</title>
</head>
<body>
    <div class="cont__retro">
        <img src="/imagenes/volver.svg" alt="" class="svg__volver">
        <p><a href="/inmobiliaria">Volver</a></p>
    </div>

        <section class="admin__segunda__seccion">
            <div class="fondo__login">

            
                <div class="cont__formulario">
                    <div>
                        <img src="/imagenes/Avatar4-min.jpg" alt="" class="img__login">
                    </div>
                    <div class="cont__admin__form">
                        <form method="POST" action="/inmobiliaria/administrador">
                            <label for="usuario">Coloque su usuario</label>
                            <input type="text" id="usuario" name="usuario">
                            <label for="password"> Coloque su contraseña</label>
                            <input type="password" id="password" name="password">
                            <button>Entrar</button>
                        </form>
                        
                    </div>
                </div>
            </div>
        </section>
    
        <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
        
        <% if(typeof alert !="undefined" ){ %>
            <script>

                Swal.fire({
                    title: "<%= alertTitle %>",
                    text: "<%= alertMessage %>",
                    icon: "<%= alertIcon %>",
                    showConfirmButton: "<%= showConfirmButton %>",
                    timer: "<%= timer %>"
                }).then(() => {
                    window.location = "/<%=ruta%>"
                })
            </script>
            <% } %>

</body>
</html>